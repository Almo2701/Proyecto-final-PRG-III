@model IEnumerable<Proyecto.Models.Cargos>

@{
    ViewBag.Title = "Index";
}

<h2>Cargos</h2>

<p>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cargo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cargo)
        </td>
        <td>
        </td>
    </tr>
}

</table>
